[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/xchat-bridge/Dockerfile"

[deploy]
healthcheckPath = "/readyz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
